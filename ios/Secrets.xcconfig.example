// Copy to `Secrets.xcconfig` (gitignored) and fill in, then run `pod install`.
//
// Google Maps / Navigation SDK key. Substituted into Info.plist's `GMSApiKey`
// at build time and read by AppDelegate.swift. The key needs the Maps SDK for
// iOS *and* the Navigation SDK for iOS enabled on its Google Cloud project,
// and must be allowed for the app's bundle identifier.
MAPS_API_KEY =
